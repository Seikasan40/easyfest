// Placeholder route group — non utilisé. Le layout est géré via root + ConditionalChrome.
export default function PassThrough({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
